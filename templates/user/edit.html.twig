{% include 'default/_form.html.twig' with {
    page_title: 'Edit a user'|trans({'%id%': category.id|default('')}),
    form: form,
    submit_label: 'edit'|trans,
    back_to_list_path: 'user_index',
} %}