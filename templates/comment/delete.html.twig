{% include 'default/_form.html.twig' with {
    page_title: 'Usuń komentarz'|trans({'%id%': comment.id|default('')}),
    form: form,
    submit_label: 'usuń'|trans,
    back_to_list_path: 'comment_index',
} %}
