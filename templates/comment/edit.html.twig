{% include 'default/_form.html.twig' with {
    page_title: 'Edytuj komentarz'|trans({'%id%': comment.id|default('')}),
    form: form,
    submit_label: 'zapisz'|trans,
    back_to_list_path: 'comment_index',
} %}