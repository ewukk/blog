{% include 'default/_form.html.twig' with {
    page_title: 'Edytuj post'|trans({'%id%': post.id|default('')}),
    form: form,
    submit_label: 'zapisz'|trans,
    back_to_list_path: 'post_index',
} %}