{% include 'default/_form.html.twig' with {
    page_title: 'Usuń post'|trans({'%id%': post.id|default('')}),
    form: form,
    submit_label: 'delete'|trans,
    back_to_list_path: 'post_index',
} %}